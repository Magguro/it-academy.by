apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarr_radarr_apps.fullname" . }}
  labels:
    {{- include "sonarr_radarr_apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonarr_radarr_apps.selectorLabels" . | nindent 4 }}
